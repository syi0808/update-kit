[package]
name = "update-kit-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
update-kit = { path = "../update-kit" }
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
